/**
 * Honesty contract — copy that flips when the backend is wired.
 * See docs/UI_FEATURES_PLAN.md §0.2.
 */

export const BACKEND_LIVE = true;

export const HERO_PRIMARY_CTA = 'Interact with Live Cluster';

export const DEMO_SECTION_HEADING = 'Live Infrastructure Modules';

export const DEMO_SECTION_SUBHEAD = 'This is not a simulation. Every interaction triggers a real command in our production .NET 9 microservices cluster. Monitor the system\'s reaction in real-time via SignalR telemetry.';

export const DEMO_FOOTER = 'All commands target production endpoints in the LHR region.';
